export const DEFAULT_SYNC_MODE = "global_on" as const

export const DEFAULT_CURRICULUM_SUBJECTS = ["Mathematics"] as const

export const DEFAULT_CURRICULUM_TOPIC_KEYS = [
  "multiplication_tables"
] as const
